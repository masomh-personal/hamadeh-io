# ThoughtfulCode — Environment Variables Template
# Copy this file to .env.local and fill in your values.
# Use the same vars in Appwrite Console → Project → Settings → Environment Variables for deployment.
#
# Local:   Copy to .env.local
# Deploy:  Add matching vars in Appwrite (NEXT_PUBLIC_* for client, APPWRITE_* for server)

# ─────────────────────────────────────────────────────────────────────────────
# Site & Branding (used for metadata, canonical URLs, etc.)
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://thoughtfulcode.dev
NEXT_PUBLIC_SITE_NAME=ThoughtfulCode
NEXT_PUBLIC_AUTHOR_NAME=Masom
NEXT_PUBLIC_AUTHOR_EMAIL=contact@thoughtfulcode.dev

# ─────────────────────────────────────────────────────────────────────────────
# Footer / Git badge (optional; empty values show "---")
# Set at build time: git rev-parse --short HEAD, git rev-parse --abbrev-ref HEAD
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_GIT_BRANCH=main
NEXT_PUBLIC_GIT_SHA=abc1234
# NEXT_PUBLIC_GIT_FULL_SHA=abc1234567890...  # full 40-char SHA; badge links to commit

# ─────────────────────────────────────────────────────────────────────────────
# Appwrite (Phase 2 — BaaS)
# NEXT_PUBLIC_* are exposed to the client; APPWRITE_API_KEY is server-only.
# Get values from Appwrite Console → Project → Settings → API Keys
# ─────────────────────────────────────────────────────────────────────────────
# NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
# NEXT_PUBLIC_APPWRITE_PROJECT_ID=your-project-id
# APPWRITE_API_KEY=your-api-key
