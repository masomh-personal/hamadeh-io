# hamadeh.io — Environment Variables Template
# Copy this file to .env.local and fill in your values.
# Use the same vars in Vercel Project Settings → Environment Variables for deployment.
#
# Local:   Copy to .env.local
# Deploy:  Add matching vars in Vercel (NEXT_PUBLIC_* for client)

# ─────────────────────────────────────────────────────────────────────────────
# Site & Branding (used for metadata, canonical URLs, etc.)
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://hamadeh.io
NEXT_PUBLIC_SITE_NAME=hamadeh.io
NEXT_PUBLIC_AUTHOR_NAME=Masom
NEXT_PUBLIC_AUTHOR_EMAIL=masom@hamadeh.io

# ─────────────────────────────────────────────────────────────────────────────
# Footer / Git badge (optional)
# Usually inferred at build time from git/CI. Only set these if you want manual overrides.
# ─────────────────────────────────────────────────────────────────────────────
# NEXT_PUBLIC_GIT_BRANCH=main
# NEXT_PUBLIC_GIT_SHA=abc1234
# NEXT_PUBLIC_GIT_FULL_SHA=abc1234567890...  # full 40-char SHA; badge links to commit

# ─────────────────────────────────────────────────────────────────────────────
# Optional backend (Phase 2 — if needed later)
# Keep backend secrets server-only and do not prefix them with NEXT_PUBLIC_.
# ─────────────────────────────────────────────────────────────────────────────
# Example:
# BACKEND_API_URL=https://api.example.com
# BACKEND_API_KEY=your-server-only-secret
