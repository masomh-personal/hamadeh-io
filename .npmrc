# Save exact versions (better for Bun compatibility)
save-exact=false

# Disable package-lock.json when using Bun
package-lock=false

# Use npm registry (same as Bun registry)
registry=https://registry.npmjs.org/
